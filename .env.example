# Optional: extra dev origins (comma-separated). Home LAN IPs are covered by next.config.ts patterns.
# For one-off testing of a single IP: ALLOWED_DEV_ORIGINS=192.168.0.249
# ALLOWED_DEV_ORIGINS=my-machine.local,192.168.50.100
